{#
/**
 * @file
 * Block template for PDB media-search island.
 *
 * Renders content only — no block label — because the component
 * renders its own "Video, Podcasts, and Rich Media" heading.
 */
#}
<div{{ attributes }}>
  {{ title_prefix }}
  {{ title_suffix }}
  {{ content }}
</div>
